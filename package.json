{
  "name": "heading-component",
  "version": "1.0.0",
  "description": "Enhance &lt;h1>, &lt;h2>, etc. for example to allow linking to them by default.",
  "main": "index.js",
  "scripts": {
    "start": "ws --cors.origin=* --port 48001",
    "test": "flow && node ./node_modules/.bin/kavun src",
    "releasable": "node ./scripts/check-todo-list.js",
    "build": "parcel build src/MoreHtmlHeading.js --no-cache --no-source-maps --experimental-scope-hoisting",
    "preversion": "npm run releasable && npm test && npm run build && git add -A dist",
    "release": "npm version major"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/more-html/heading-component.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/more-html/heading-component/issues"
  },
  "homepage": "https://github.com/more-html/heading-component#readme",
  "devDependencies": {
    "flow-bin": "^0.77.0",
    "kavun": "^1.0.0",
    "local-web-server": "^2.6.0",
    "parcel-bundler": "^1.9.7"
  },
  "dependencies": {}
}
